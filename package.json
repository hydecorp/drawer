{
  "name": "hy-drawer",
  "version": "1.0.0-stencil.0",
  "description": "A touch-enabled drawer component for the modern web",
  "module": "dist/esm/index.js",
  "main": "dist/index.js",
  "unpkg": "dist/index.js",
  "types": "dist/types/components.d.ts",
  "collection": "dist/collection/collection-manifest.json",
  "files": [
    "dist",
    "lib",
    "src"
  ],
  "scripts": {
    "clean": "rm -rf dist lib",
    "test": "exit 0",
    "build": "npm run build:pencil && npm run build:stencil",
    "watch": "npm run watch:pencil",
    "build:pencil": "find src -name '*.js' -exec cp {} lib \\; && tsc -d --outDir lib",
    "watch:pencil": "find src -name '*.js' -exec cp {} lib \\; && tsc -d --outDir lib -w",
    "build:stencil": "stencil build --docs",
    "watch:stencil": "stencil build --dev --watch --serve",
    "build-docs": "stencil build --docs",
    "preversion": "sed -i '' 's:/dist:#/dist:' .gitignore",
    "version": "npm run build && git add .",
    "postversion": "sed -i '' 's:#/dist:/dist:' .gitignore && git rm --cached -r dist && git add . && git commit -m 'restore preversion .gitignore'"
  },
  "author": "Florian Klampfer <mail@qwtel.com> (https://qwtel.com/)",
  "license": "GPL-3.0",
  "devDependencies": {
    "@stencil/core": "^0.18.1",
    "rollup": "^1.7.4",
    "rollup-plugin-commonjs": "^9.2.2",
    "rollup-plugin-node-resolve": "^4.0.1",
    "rollup-plugin-typescript": "^1.0.1"
  },
  "dependencies": {
    "pencil-runtime": "^0.2.0",
    "rxjs": "^6.4.0",
    "rxjs-create-tween": "^2.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/qwtel/hy-drawer.git"
  },
  "bugs": {
    "url": "https://github.com/qwtel/hy-drawer/issues"
  },
  "homepage": "https://qwtel.com/hy-drawer/"
}
